<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:colorPrimary">@color/primary_brand</item>
        <item name="android:colorButtonNormal">@color/primary_brand</item>
        <item name="colorPrimary">@color/primary_brand</item>
        <item name="colorPrimaryDark">@color/primary_brand</item>
        <item name="android:colorBackground">@color/background_main</item>
        <item name="android:textColorPrimary">@color/primary_text</item>
        <item name="android:editTextColor">@color/text_color</item>
    </style>


</resources>